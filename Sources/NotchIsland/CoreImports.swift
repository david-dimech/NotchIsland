// Re-export NotchIslandCore so every file in this target sees its public
// types (GmailMessage, AlertInfo, AlertManager, MusicTheory, …) without
// needing an explicit `import NotchIslandCore`.
@_exported import NotchIslandCore
